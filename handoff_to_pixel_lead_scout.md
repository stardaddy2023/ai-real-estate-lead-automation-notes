# Task Brief: Lead Scout Refinement

> [!IMPORTANT]
> **To:** PIXEL (Frontend Engineer)
> **From:** ARCHITECT
> **Priority:** High (Feature Restoration)

## Objective
Reintegrate **Map View** and **Search** into the Lead Scout.

## Instructions
1.  **Create `LeadMap.tsx`:**
    *   Use `react-leaflet`.
    *   Render pins for each lead.
    *   Popup shows Address + Status.

2.  **Update `LeadInbox.tsx`:**
    *   **Search Bar:** Add an input field to filter the `data` array by address.
    *   **View Toggle:** Add Tabs or Toggle for "List" vs "Map".
    *   **State:** Maintain `view` state ('list' | 'map').

## Handoff
Mark task `Reintegrate Map & Search` as `[x]` when done.
